name: Git Bundle Checkout
description: Seeds the Buildkite checkout from a GCS-hosted git cache tarball (fast, in-infra) instead of cloning from GitHub
author: Hivebrite
requirements:
  - gcloud
  - git
  - tar
configuration:
  properties:
    gcs_uri:
      type: string
  required:
    - gcs_uri
